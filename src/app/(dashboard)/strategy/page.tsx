import { StrategySection } from "@/components/sections/StrategySection";

export const metadata = { title: "Strategy — Trading Dashboard" };

export default function StrategyPage() {
  return <StrategySection />;
}
