import { RoutineSection } from "@/components/sections/RoutineSection";

export const metadata = { title: "Daily Routine — Trading Dashboard" };

export default function RoutinePage() {
  return <RoutineSection />;
}
